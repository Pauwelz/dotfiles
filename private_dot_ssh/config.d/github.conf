# Keys live in the Bitwarden SSH agent; the .pub file (written by chezmoi from
# the vault) tells ssh which agent key to offer under IdentitiesOnly.
Host github.com
    User git
    IdentityFile ~/.ssh/id_ed25519.pub
